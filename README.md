# 🚀 Dockerized React App

This repository contains a **Dockerized React application**. I cloned this project to **practice containerizing frontend applications using Docker**, and to learn the process of building and serving a production-ready React app.

> 🧠 Original credits go to the developer from whom this project was cloned. This fork is for **educational purposes only**.

---

## 🐳 Dockerfile

The app uses a `Dockerfile` to build and serve the React project inside a container.
